import styled from "styled-components";

const Main = () => {
    return (
        <Container>Main Side</Container>
    );
}

const Container = styled.div`
    grid-area: "main"
`

export default Main